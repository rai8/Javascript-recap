//primitive values are always immutable , can never be changed once they are declared
let age = 43;
age = 76; // this will create a new variable but not modifying the previous age declared
console.log(age);

/* Nan- this always acts as a placeholder



 */
